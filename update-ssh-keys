#!/bin/sh

for f in authorized_keys.d/*; do
	user=$(basename $f .pub);
	user=${user%@*}
	echo -n "restrict,pty,command=\"$PWD/bin/fram $user\" "
	head -n 1 $f
done > .ssh/authorized_keys

chmod 600 .ssh/authorized_keys
